#include <string>
#include <vector>

//#include "page.cpp"
#include "story.cpp"
using namespace std;

int main(int argc, char ** argv) {
  if (argc != 2)
    perror("not enough arguments\n");

  Story new_story(argv[1]);

  return EXIT_SUCCESS;
}
